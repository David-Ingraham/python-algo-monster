class Solution:
    def maxNumberOfBalloons(self, text: str) -> int:
        word = balloon
        instance = 
        count = 0

        for char in text:
            if char in word:
                instance = instance + char
                text = text.replace(char, )
                print(instance)
                print(text)
            
            if instance == word:
                count +=1
                instance = 
        return count
            

